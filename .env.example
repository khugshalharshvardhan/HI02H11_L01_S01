# Template — copy to .env and fill in. Committed on purpose; .env is not.
#
# The asset generators read GEMINI_KEY from the ENVIRONMENT, not from this file.
# See .env for the export commands.

GEMINI_KEY=
GEMINI_API_KEY=
